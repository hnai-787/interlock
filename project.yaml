name: Interlock
slug: interlock

category: experiments
subcategory: "Digital Logic Design"

status: completed

visibility: public

description:
  short: "An NE555-timer and D-flip-flop-based 4-way traffic signal controller, verified in Proteus simulation and on physical hardware."
  long: |
    A 4-way traffic signal controller built entirely from discrete digital
    logic: an NE555 astable timer providing a ~1Hz clock, a 4-bit D-flip-flop
    counter cycling through 16 states, and combinational logic decoding
    those states into 12 LED outputs (red/yellow/green per road). A full
    cycle takes 16 seconds (4 seconds per road: 3s green + 1s yellow).
    Verified both in Proteus simulation and on a physical breadboard/LED
    prototype, plus a programmatic cross-check of every simplified
    Boolean equation against the truth table (0 mismatches across all 16
    states) and of the no-double-green safety property (0 violations).
    Includes a timed live-demo script (DEMO.md).

technologies: ["Proteus", "NE555 timer", "Digital logic (D flip-flops)"]
tags: ["experiments", "digital-logic", "hardware", "circuit-design"]

repository:
  provider: github
  visibility: public
  sync: true

portfolio:
  enabled: true
  featured: true

documentation:
  readme: true
  case_study: false

dates:
  started: null
  completed: null

links:
  github: https://github.com/Hussain-Ali-787/interlock
  demo: null
  documentation: null

assets:
  hero: assets/brand/interlock-readme-banner.png
  screenshots:
    - screenshots

achievements: []

metrics:
  - label: "Verification checklist items passed"
    value: "all (timer period, road sequencing, hardware confirmation)"
    verified: true
  - label: "Boolean equations matching truth table (programmatic cross-check, all 16 states)"
    value: "16/16, 0 mismatches"
    verified: true
  - label: "No-double-green safety property (programmatic check, all 16 states)"
    value: "0 violations"
    verified: true
